node_modules/
dist/
vendor/
*.log
data/
.DS_Store
# build/ 只保留图标（打包必需），其余构建产物不入库
build/*
!build/icon.ico
!build/icon.png
# 内置 bundle 插件的 dist 产物必须入库：否则全新环境（clone/解压）下入口文件
# 缺失，服务重启必失败、Web UI 不可用（issue #65）。这两处是随包分发的可装配
# bundle，例外于通用的 dist/ 忽略规则。
!assets/plugins/graph-memory/dist/
!assets/plugins/billion-context-dsh/dist/
