[package]
name = "kernel-process"
description = "DSH 内核进程保姆：spawn 规格 / 就绪行解析 / 崩溃环 / 端口选择"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
